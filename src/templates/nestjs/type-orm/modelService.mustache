import {
  HttpException,
  HttpStatus,
  Inject,
  Injectable,
  Res,
} from '@nestjs/common';
{{&routeDto}}
{{&routeEntityDirect}}

  @Injectable()
  export class {{tableClassService}} {
    constructor(
    @InjectRepository({{tableEntity}})
    private readonly {{tableClassrepository}}: typeof {{tableEntity}},
  ) {}
    async findAll() {
      const _{{tableCamelCase}} = await this.{{tableClassrepository}}.find<any>();
      return _{{tableCamelCase}}.map({{tableInitial}} => new {{tableModelDto}}({{tableInitial}}));
    }
  };



